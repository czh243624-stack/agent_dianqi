# 易发式电气 · 询盘回复工作台 · 交接文档

> 更新日期：2026-08-12  
> 范围：一期只做「询盘回复」；短视频 / 内容推广已关闭  
> 仓库：`D:\agent_haiwai`

---

## 1. 一句话说明

给海外销售用的**询盘回复工作台**：买家询盘进来 → 看原文 → 整理参数与参考资料 → 生成英文草稿 → **人工确认后**再模拟发送。  
不做自动报价，不自动承诺认证/交期。

用户界面文案避免写「Agent / RAG / Mock」等黑话；对内可叫参考资料检索。

---

## 2. 日常怎么打开（先记这个）

| 用途 | 地址 | 说明 |
|------|------|------|
| **运营大屏（首页）** | http://127.0.0.1:5173/ | AI Agent 运营态势；右上角「进入工作台」 |
| **工作台** | http://127.0.0.1:5173/inquiries | 若依风格管理后台 |
| 询盘 API（后端） | http://127.0.0.1:3001/api/health | **没有网页**；根路径 `/` 会 404，属正常 |
| 演示独立站（买家站） | http://127.0.0.1:5175/ | 填表提交后进工作台「独立站」来源 |

路由：`/` 大屏 · `/inquiries` 工作台 · `/customers` 客户池 · `/knowledge` 知识库 · `/logs` 日志。
大屏数据全部来自真实接口汇总，每 30 秒自动刷新。


**5173 和 3001 不是同一个东西：**

```text
浏览器 → 5173 前端页面
              ↓ 自动把 /api 转到
            3001 后端接口
```

工作台能刷出列表，说明后端已在工作。不要把 3001 当网站首页访问。

---

## 3. 本地启动

需要本机已装 Node.js（建议 20+）。

```bash
# 1) 后端
cd D:\agent_haiwai\server
npm install
npm run setup          # prisma generate + migrate/seed（首次或库重置时）
npm run dev            # → 3001

# 2) 工作台
cd D:\agent_haiwai\demo
npm install
npm run dev -- --host 127.0.0.1 --port 5173   # → 5173

# 3) 演示独立站（可选，演示网站进盘时再开）
cd D:\agent_haiwai\website-demo
npm install
npm run dev            # → 5175
```

健康检查：

```bash
curl http://127.0.0.1:3001/api/health
# 期望：{"ok":true,"service":"transformer-agent-server",...}
```

---

## 4. 业务主流程

```text
接入询盘
  → 列表（来源 / 未回复·已回复 / 搜索 / 时间排序 / 分页）
  → 详情看买家原文
  → 开始整理（分析）
  → 审核：改英文草稿、看参考资料、通过或退回
  → 确认发送（当前为模拟发送，状态变为「已回复」）
```

工作台左侧步骤条对应：

1. 查看询盘  
2. 整理参数与资料  
3. 人工确认  
4. 确认发送  

### 接入方式（左侧栏）

| 入口 | 行为 |
|------|------|
| 手动粘贴询盘 | 粘贴英文原文 → 保存 → 再分析 |
| 同步阿里通知 | Stub：从 `integrationInbox` 拉阿里样例 |
| 打开演示独立站 | 打开 `5175`，表单 POST 到 webhook |
| 刷新列表 | 重新拉 `GET /api/inquiries` |

**已去掉：邮箱收件渠道**（不再有「拉取邮箱」）。  
买家「联系邮箱」字段仍保留；发送仍走出站模拟记录。

### 回复状态

- **未回复**：`status !== sent`  
- **已回复**：`status === sent`  
- 列表可筛：全部 / 未回复 / 已回复  

---

## 5. 仓库结构

```text
agent_haiwai/
├── demo/                 # 工作台前端 Vite + React + TS
│   ├── src/App.tsx       # 主流程 UI
│   ├── src/api.ts        # 调 /api
│   ├── src/App.css       # 样式（系统字体栈）
│   └── vite.config.ts    # /api 代理到 3001
├── server/               # 询盘 API Fastify + Prisma + SQLite
│   ├── prisma/           # schema + seed
│   └── src/
│       ├── routes/index.ts
│       ├── services/inquiryAgent.ts   # 分析 / 草稿 / 来源
│       ├── services/agentConfig.ts
│       └── adapters/                  # 进盘与出站
│           ├── websiteForm.ts         # 独立站 Webhook（可用）
│           ├── alibaba.ts             # Stub
│           └── email.ts               # 仅模拟出站，不做收件
├── website-demo/         # 演示买家独立站（英）
├── video-agent-demo/     # 短视频相关（一期关闭，可忽略）
├── PRODUCT.md            # 产品边界
├── DESIGN.md             # 视觉约定
└── README.md             # 简短启动说明
```

---

## 6. 关键接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |
| GET | `/api/inquiries` | 列表：`page` `pageSize` `channel` `reply` `q` `sort` |
| GET | `/api/inquiries/:id` | 详情（含 sources） |
| POST | `/api/inquiries` | 手动创建 |
| POST | `/api/inquiries/:id/analyze` | 分析 + 匹配资料 + 草稿 |
| PATCH | `/api/inquiries/:id` | 改草稿等 |
| POST | `/api/inquiries/:id/approve` | 通过 |
| POST | `/api/inquiries/:id/reject` | 退回 |
| POST | `/api/inquiries/:id/send` | 模拟发送 |
| POST | `/api/webhooks/website-form` | 独立站进盘 |
| POST | `/api/integrations/alibaba/sync` | 阿里 stub 同步 |
| GET/PATCH | `/api/agents` … | 模块配置（询盘启用；视频/SEO 默认关） |

独立站表单字段：`company` `name` `email` `country` `product` `message`。

---

## 7. 当前实现水位（交接重点）

### 已可用

- 询盘列表：来源筛选、回复状态、搜索、排序、分页、收到时间  
- 详情原文 → 规则引擎分析 → 审核台改稿 → 通过/退回 → 模拟发送  
- 参考资料引用位：`InquirySourceHit`（产品/认证/交期/历史报价等）  
- 演示独立站进盘闭环  
- 设置页可改询盘模块说明 / systemPrompt（生成尚未真正接 LLM）  

### 刻意关闭 / 不做（一期）

- 短视频制作、内容推广入口  
- 邮箱 IMAP 收件  
- 阿里 Open API 直连自动回  
- 自动对外报死价 / 自动承诺认证交期  

### 半成品 / 下一刀建议

| 项 | 现状 | 建议 |
|----|------|------|
| 分析与草稿 | 正则 + 模板 | 接 LLM；草稿只许用检索到的事实 |
| 参考资料（对内称 RAG） | 库表精确匹配，非向量检索 | 先做 corpus + 关键词检索 MVP，再 embedding |
| 发信 | mock 写出站记录 | 以后换真 SMTP；发送成功才标已回复 |
| 阿里 | stub 收件箱 | 权限未定时继续用通知粘贴 |

**RAG 不是再造一个神秘库**：是「先查自家产品/认证/交期/FAQ，再写回信」。数据库只是资料存放方式之一。  
详细落地口径此前已对齐：P1 语料+关键词 → P2 向量+LLM → P3 运营后台。

---

## 8. 演示路径（给评审 / 新人）

1. 开齐三个服务（至少 5173 + 3001；要演示网站进盘再开 5175）  
2. 打开工作台 → 询盘列表  
3. 左侧「打开演示独立站」→ 填 RFQ 提交  
4. 回工作台「刷新列表」→ 来源选「独立站表单」应看到新单（未回复）  
5. 点进详情 →「开始整理」→ 审核页改草稿 → 通过 → 确认发送 → 变为已回复  

备用：左侧「手动粘贴询盘」贴一段英文询盘同样可走完全程。

---

## 9. 数据与配置

- DB：SQLite（Prisma），路径见 `server/.env` / `schema.prisma`  
- 重置演示数据：在 `server` 下按项目脚本执行 seed（见 `package.json` 的 `db:seed` / `setup`）  
- 默认询盘 Agent：`inquiry_reply`（enabled）  
- 视频/SEO Agent：默认 `enabled: false`  
- 渠道配置：`manual` / `website_form` / `alibaba`（已移除 email 进盘）  

---

## 10. 设计与文案约定

- 工作台：深色操作台 + 青绿强调色；字体为系统栈（PingFang / 微软雅黑等）  
- 品牌对外演示站：Yifa Electric（`website-demo`）  
- 产品名：易发式电气工作台 / Yifa Export Desk  
- 详见 `PRODUCT.md`、`DESIGN.md`  

---

## 11. 常见问题

**Q：打开 3001 是空白 / 404？**  
A：正常。请访问 `/api/health`，或只用 5173。

**Q：独立站打不开？**  
A：确认 `website-demo` 已 `npm run dev`，端口为 **5175**（不是 5174）。

**Q：列表没有独立站来的单？**  
A：确认 API 在 3001；独立站经 Vite 代理打 `/api/webhooks/website-form`；工作台点刷新，来源筛「独立站表单」。

**Q：分析很「死板」？**  
A：当前是规则引擎，不是大模型。接 LLM + 参考资料检索是下一阶段。

**Q：发送会真发到客户吗？**  
A：不会。当前为模拟发送，只改状态并记出站记录。

---

## 12. 推荐接手顺序

1. 本地按 §3 跑通，走一遍 §8 演示  
2. 读 `inquiryAgent.ts`（分析/草稿/来源）和 `routes/index.ts`  
3. 若做资料检索：加 `corpus/` + `server/src/rag/`，改 `analyzeInquiry` 走检索  
4. 若接真通道：只换 `adapters/`，尽量不改路由形状  

---

## 13. 相关文档

| 文件 | 内容 |
|------|------|
| `PRODUCT.md` | 产品边界与用户任务 |
| `DESIGN.md` | 视觉约定 |
| `README.md` | 最短启动说明 |
| `server/README.md` | 后端适配器现状 |
| 本文 `交接文档.md` | 完整交接 |

如有冲突，以**本仓库当前代码行为**为准；产品边界以 `PRODUCT.md` 为准。
