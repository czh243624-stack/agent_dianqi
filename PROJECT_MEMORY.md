# 项目记忆

## 2026-08-12：AI 模型接入原则

第一个 Agent 是“变压器外贸询盘回复 Agent”。正式版 AI 部分不使用规则兜底。

已确认原则：

- AI 未配置 API Key 时，分析/生成流程必须直接报错，提示“AI 模型未配置”。
- AI API 调用失败时，必须直接报错，提示“AI 服务调用失败”。
- AI 返回格式不合格时，必须直接报错，提示“AI 输出解析失败”。
- 不允许在用户以为 AI 工作时，后台偷偷回退到规则模板。
- 一期不训练模型。
- 一期通过 Prompt + 企业知识库/RAG + 严格输出格式，让通用大模型理解当前业务。
- 模型本身不知道企业产品、认证、报价规则和销售口径，系统每次调用都必须把询盘、企业资料、任务规则、输出格式和风险约束传给模型。
- 价格、交期、认证范围、测试承诺必须保留人工审核，AI 不得直接对外承诺。

建议模型接入顺序：

1. 先接 DeepSeek 或 Qwen API，适合国内调试和成本控制。
2. 对英文质量要求高时，可接 OpenAI / Claude。
3. 私有化部署需求明确后，再考虑本地模型。

下一步开发口径：

- 先完成后端 AI 模型配置和前端配置入口。
- 再接真实模型适配器。
- 接入真实模型后，移除当前规则分析在正式分析链路中的兜底作用。

## 2026-09-01：易发式电气客户信息表

客户提供了桌面文件《易发式电气外贸 AI Agent 信息填写表 (1).docx》，该文件作为第一个 Agent 的正式业务资料来源。

已整理并导入的信息：

- 公司名称：辽宁易发式电气设备有限公司 / Liaoning EFACEC Electrical Equipment Co., Ltd.
- 官网：http://www.leeec.com；英文站：new.leeec.com。
- 重点市场：非洲、中东/GCC；拓展市场：东欧、南欧、东南亚、南美、俄罗斯。
- 一期只做变压器外贸询盘回复 Agent，不做 SEO、视频、社媒发布。
- 产品范围包括电力变压器、油浸式电力变压器、干式变压器、配电变压器、三绕组变压器、有载调压变压器、电抗器、移动变电站、预装式变电站、箱式变电站和特殊变压器方案。
- AI 必须识别买家意图、变压器类型、应用场景、报价条件、缺失参数、A/B/C 客户等级和销售跟进建议。
- 关键追问字段包括 Rated voltage、HV、LV、Capacity、Frequency、Vector group、OLTC requirement、Impedance、IEC standard、Installation altitude、Quantity、Destination、Delivery、Certification。
- 认证、价格、交期、付款、质保、性能保证、合同承诺必须人工审核。
- 阿里国际站自动化仍需客户确认主账号/子账号权限、询盘读取权限、消息回复权限、客户管理权限、Open API、message/chat API、webhook。
- 独立站自动化仍需客户确认后台地址、技术联系人、表单字段能否修改、是否可配置 webhook、是否已有询盘数据库和状态同步需求。

项目内资料文件：`CLIENT_INFO_SUMMARY.md`。
后端非破坏性导入脚本：`server/scripts/import-leeec-client-info.ts`，命令 `npm run import:leeec`。

## 2026-09-01：易发式电气独立站公开资料

已访问并整理官网 `https://www.leeec.com/` 的公开信息，作为第一个 Agent 的 RAG 公开资料来源。英文站 `new.leeec.com` 当前无法通过抓取工具打开，后续需要客户确认可访问地址或后台资料。

官网公开公司资料：

- 公司位于辽宁省辽阳市，是省级技术中心和高新技术企业；2022 年被认定为省级“专精特新”中小企业。
- 成立于 1994 年 10 月，注册资本 10000 万元，总资产 10.5 亿元。
- 占地面积 64000 平方米，建筑面积 33528 平方米，厂房建筑面积 30559 平方米。
- 拥有 669 台套设备，其中 411 台套来自美国、英国、德国、意大利、葡萄牙等 11 个国家。
- 员工 310 人。

官网公开产品能力：

- 370MVA/500kV 及以下油浸电力变压器。
- 6.3MVA/35kV 及以下油浸配电变压器。
- 3.15MVA/35kV 及以下环氧树脂浇注干式变压器。
- 移动变电站、预装式变电站、箱式变压器、EPC 总承包工程项目。

官网产品知识：

- 电力变压器：设计强调优化计算、电场/磁场/温度场/冲击电压分析；技术卖点包括减少局部放电、防漏油、降噪、耐突发短路。页面列出 330kV、220kV、110kV、66kV 案例和多国出口/客户参考。
- 干式变压器：耐火、无污染、耐腐蚀、防潮、免维护、低损耗、低局放、低噪音、机械强度高、抗短路能力强；应用于输配电、商业建筑、地铁、机场、石化、海上钻井平台、负荷中心和消防要求高的场景。
- 移动变电站：集成变压器、一次设备、二次保护和控制设备，适用于应急、检修、临时、建筑用电场景。
- 预装式变电站：高压模块、主变模块、低压模块组成；官网提到工厂周期 3 个月、现场施工周期 1 个月，但不能直接承诺给客户。
- 箱式变压器：分美式和欧式，应用于城市配电、路灯、工矿、住宅、山区、酒店、公园、建筑工地、机场、油田、码头、高速公路等。

官网认证/荣誉线索：

- 企业简介提到 ISO9001-2000、ISO14001、GB/T 28001-2001、17 个国家国际多边认证，CTQC、SGS、VEIKI-VNL、KEMA、ASTA 等 50 余台变压器试验认证。
- 资质页列出 ASTA 100M/220kV、KEMA 180MVA/220kV、莱茵和 CTQC 多个电压/容量等级认证线索。
- 这些只作为公开线索，不等于当前可对外承诺的认证范围；正式回复必须核验证书原件、适用型号和有效期。

官网联系方式：

- 地址：中国辽宁省辽阳市白塔区北园路 29 号。
- 电话：+86-419-3130233；传真：+86-419-3133643。
- 国内售后：86 18841941368；沙特售后：966 506648950；联系人：边禹。
- 沙特办事处：Al Dabab, Dammam 32261 沙特阿拉伯。
- 官网邮箱抓取结果为遮罩文本 `[email protected]`，不能作为正式销售邮箱写入草稿。

已将官网信息追加到 `server/scripts/import-leeec-client-info.ts`，并以 `official_website_*` 类型导入知识库；仍需区分“官网公开资料”和“客户填写表资料”。

用户确认“独立站里都有需要的信息”。因此后续优先把 `https://www.leeec.com/sitemap.xml` 作为公开官网资料采集入口，自动抓取官网公开页面进入 RAG 知识库。注意：这是官网公开资料采集，不等于阿里国际站权限、独立站后台权限、历史报价、真实询盘、客户沟通记录已经具备。

已新增脚本：`server/scripts/crawl-leeec-website.ts`，命令 `npm run crawl:leeec-site`。脚本会从 sitemap 读取公开页面，清洗正文并导入 `KnowledgeDocument`，类型包括 `official_website_product_page`、`official_website_certification_page`、`official_website_news_page`、`official_website_company_page`、`official_website_contact_page` 等。

## 2026-09-01：已把现有资料做到第一个 Agent

当前第一个 Agent 已集成客户表和官网公开资料：

- `server/scripts/import-leeec-client-info.ts` 会把公司配置、产品库、认证/资质库和资料库导入开发数据库，不再导入测试询盘。
- 产品库已补入官网公开能力范围：370MVA/500kV 油浸电力变压器、6.3MVA/35kV 油浸配电变压器、3.15MVA/35kV 干式变压器、移动变电站、预装式变电站、箱式变压器，以及 SCB10、SSZ22 和官网案例产品。
- 认证库已补入 ISO9001、ISO14001、GB/T28001、国际多边认证线索、CTQC、KEMA、ASTA、TUV Rheinland、SGS、VEIKI-VNL 等公开线索，全部标记为需人工核验证书原件和适用范围。
- 资料库已补入 `official_website_*` 官网知识，包括公司规模、产品能力、出口市场、产品页知识、联系方式和 2026 扩建公告。
- 识别器已支持移动变电站、预装式变电站、箱式变压器、三绕组变压器、有载调压变压器和电抗器。
- 评级规则调整：明确产品方向但参数不完整的询盘优先归 B 级，继续追问补齐；A 级仍要求项目、参数、数量、目的地、交期和联系方式较完整。

用户要求删除之前的 demo 假数据。已新增清理脚本 `server/scripts/cleanup-demo-data.ts`，命令 `npm run cleanup:demo`。清理范围包括早期假客户、假询盘、假产品型号、假认证、假历史报价、mock 邮件、demo 集成收件箱和早期 v1 示例知识库；保留客户表、官网采集和公开资料导入内容。

## 2026-09-01：AI 助手定位修正

用户明确“AI 助手”不是 Agent 功能说明页，而是后台业务问答助手，类似在系统里问：“最近 30 天收到了多少询盘”“A级客户有哪些”“哪些客户要优先跟进”“哪些变压器类型咨询最多”。

已按此定位开发：

- 前端菜单 `/ai-assistant` 改为聊天式业务问答页面。
- 后端新增 `GET /api/ai-assistant/snapshot`，读取真实业务数据快照。
- 后端新增 `POST /api/ai-assistant/chat`，把真实数据快照交给已配置的大模型回答。
- 问答不使用规则兜底；未配置 API Key、API 调用失败或模型输出异常时直接报错。
- 当前系统没有订单、回款、成交金额表，所以“业绩”只能按询盘量、待处理量、A/B/C 客户、发送状态等业务过程数据回答，不能编造销售额。

后续 UI 调整：

- AI 助手页删除顶部“模型配置”按钮和右侧“刷新”按钮。
- 右上角改为语言选择，可切换中文 / English。
- 语言会影响页面文案、快捷问题和 AI 回答语言。
- 中文模式下，产品型号、产品类型、认证名称、Lead Grade A/B/C、RAG、HV/LV、Capacity、Frequency、Vector Group、OLTC、Impedance、IEC 等硬性业务数据和技术术语必须保留英文，不要强行翻译。

## 2026-09-01：运营大屏语言切换

用户要求运营大屏也支持中英文切换，并删除顶部“刷新数据”和“AI配置”入口。

已完成：

- 运营大屏右上角新增语言选择：中文 / English。
- 删除运营大屏顶部“刷新数据”按钮和“AI配置”链接。
- 大屏仍保留自动刷新机制和真实设置读取，页面不再暴露手动刷新按钮。
- 切换语言只影响界面文案；产品类型、客户名称、国家、产品型号、技术术语、A/B/C、RAG 等硬性业务数据保持原样。

## 2026-09-02：运营大屏按实际业务使用重构

用户纠正：运营大屏是给客户实际日常使用的，不是给客户看的项目实施/上线准备页面。因此大屏不能展示“一期能力、资料完善、项目实施进度、上线前还缺什么”等内容，这些与业务使用关系弱，也容易暴露内部交付信息。

当前运营大屏定位调整为“变压器外贸询盘运营”：

- 首屏核心指标：累计询盘、待处理、待审核草稿、A级客户、客户池。
- Agent 作用用业务结果体现：自动识别 Product Type、Application Scenario、Missing Parameters、Lead Grade，而不是显示模型配置或实现细节。
- 业务图表应围绕销售运营：询盘处理闭环、客户质量分布、变压器需求分布、重点市场来源、应用场景识别、渠道来源、最常缺失报价参数、最近进入询盘。
- 所有数据必须来自后端真实接口；没有真实询盘时显示暂无真实数据，不能用假数据填充。
- RAG、产品库、认证资料是系统后台支撑，不应该作为客户大屏主视觉。除非客户明确要求查看知识库资产，否则不要放在运营大屏首屏。

## 2026-09-02：语言切换必须持久化

用户反馈：运营大屏设置成英文后，刷新页面又回到中文。原因是页面语言只保存在 React 内存状态中，刷新后默认初始化为中文。

已修正：

- 新增 `demo/src/languagePreference.ts`，使用 `localStorage` 保存用户选择。
- 运营大屏和 AI 助手共用同一个语言偏好。
- 用户选择英文后，刷新页面、重新进入页面、从大屏进入 AI 助手，都应保持英文；选择中文同理。

## 2026-09-02：客户资料知识库补全

用户要求核对“给的信息里面有用的知识库是否都写进去”。重新读取原始填写表后发现早期整理有遗漏和一处市场口径错误，已修正并重新导入数据库。

关键修正：

- 原始填写表中重点市场是北美、中东/GCC；拓展市场是南欧、东欧、南美、非洲；俄罗斯是不作为目标市场。此前误把非洲放入重点市场、俄罗斯放入拓展市场，已修正系统设置和知识库。
- 产品库从 12 条补到 17 条，新增/补强 `11-750kV Power Transformers`、`800MVA/750kV` 能力口径、`Reactors`、`OLTC transformer`、`Special transformer solutions`，并补齐 `SSZ22 Three-Winding Power Transformer` 的 `360MVA/220kV` 结构化字段。
- 认证库从 10 条补到 15 条，新增 `GB/T23331`、`GB/T39604`、`ISO/IEC information security management system`、`ISO28000`、`PCCC`。这些全部按“体系/认证线索，需核验证书原件、有效期和适用范围”处理。
- 知识文档从 230 条补到 234 条，新增区域客户画像、750kV 试验能力与报告回复规则、包装运输与售后服务口径、风险客户/低优先级线索规则。
- 官网 sitemap 在 2026-09-02 重新抓取时扫描 236 个页面，121 个成功更新，115 个返回官网侧 `520`。旧数据未删除，当前官网资料仍保留 219 条；后续生产化应给爬虫加限速、重试和失败队列。
- 检索逻辑已修正：`KnowledgeDocument` 不再只取最近 50 条，而是纳入全部知识文档；并给 `market_customer_profile`、`testing_capability`、`packing_shipping_after_sales`、`risk_customer_policy` 等新增业务知识加检索权重，避免被官网页头/页脚重复文本挤出结果。

当前数据库快照：产品 17 条、认证 15 条、知识文档 234 条。

## 2026-09-02：资料库分页

用户要求企业知识库里的“资料库”不能全部显示在一页，需要支持翻页，并可设置每页 10、20、50 行，默认 10 行。

已实现：

- 后端 `GET /api/knowledge-documents` 支持 `page` 和 `pageSize` 查询参数。
- 返回结构包含 `items`、`page`、`pageSize`、`total`、`totalPages`。
- 前端“企业知识库 > 资料库”默认每页 10 条，可切换 10/20/50 行。
- 产品库和认证资料当前条数较少，暂未分页；后续数据量增加时可复用同样模式。

## 2026-09-02：客户可见文案和资料库搜索修正

用户要求继续清理客户实际使用界面，不能把内部实施问题、上线缺口、风险提示当成大屏业务内容展示。

已确认并调整：

- 运营大屏公司名使用 `Liaoning EFACEC Electrical Equipment Co., Ltd.`。
- 运营大屏中文标题使用 `易发式电气外贸 Agent`。
- 删除运营大屏“发送前风险”模块，不再展示报价、交期、认证人工确认提示。
- 左侧菜单顺序改为“询盘工作台”在“AI 助手”前面。
- AI 助手常见询问只能放客户日常业务问题，例如最近 30 天询盘、待回复询盘、A级客户、咨询最多的变压器类型、缺失报价参数、国家/地区来源；不要再出现“上线缺口”“知识库准备情况”等内部交付问题。
- 企业知识库的“资料库”支持关键词搜索，和分页联动；搜索范围包括类型、标题、标签、正文、版本、可见性。
- 客户可见界面和 AI 助手回答里不要出现“一期”“本期”“Phase 1”“上线缺口”等项目阶段/交付管理字眼。顶栏统一使用“外贸询盘 Agent”，旧入口说明也按正式业务工作台口径展示。

## 2026-09-03：询盘 AI 语义分析与 Skill 化

用户明确要求：AI 自动分析询盘必须理解买家语义，不能只因为文本里出现某个产品类型、参数或认证关键词，就直接判断为明确需求。

已调整为：

- 询盘分析主流程改为调用已配置的大模型 API，API Key 未配置、API 调用失败、AI 输出格式错误时直接报错，不启用规则兜底。
- 原本本地关键词/正则分析器不再被 `analyzeInquiry` 主流程调用。
- AI 分析必须区分 `explicit`（买家明确提供）、`inferred`（语义推断）、`mentioned_not_requirement`（只是提到但不是需求）、`missing`（缺失）。
- 产品类型和应用场景可以语义推断，但必须输出证据、置信度，并标注是否需要业务员确认；HV/LV、Capacity、Frequency、Vector Group、OLTC、Impedance、IEC、Installation Altitude、Quantity、Destination 等技术/报价参数不得凭空推断。
- 已新增 6 个询盘 Agent Skill：询盘语义理解、变压器类型判断、技术参数抽取、缺失参数追问、客户价值评级、回复策略。
- 后端新增 `GET /api/agent-skills`，返回当前 Agent Skill 清单。
- 询盘详情页 AI 分析 Tab 展示语义摘要、意图/类型/场景置信度、产品类型是否需确认，以及每个 Skill 的执行结果和依据。
- 参数表增加来源、解释类型、置信度、判断依据，方便业务员看出哪些是买家原文、哪些是 AI 语义推断。
- 英文回复草稿也改为大模型基于“语义分析 + RAG 检索资料”生成，不再用固定模板兜底。
- 配置 DeepSeek 后已造两条测试询盘完成链路自测：明确 RFQ 被识别为 A 级，产品类型/HV/IEC 等参数标记为 `buyer_text / explicit`；室内商场场景被识别为 Dry-type 方向但标记为 `semantic_inference / inferred / needsConfirmation=true`，没有把“顾问询问 oil type 是否可接受”误判成明确油浸式需求。两条测试均生成 6 个 Skill 结果、8 条 RAG 参考和英文回复草稿。

## 2026-09-03：Agent Skill 可用性优化

用户反馈原 6 个 Skill 偏展示，真实销售使用价值不足。已把 Skill 从“说明 AI 做了什么”调整为“销售下一步工作单”。

当前询盘 Agent 使用 8 个 Skill：

- `buyer_intent_stage`：买家意图与阶段，判断是否正式 RFQ、技术选型、供应商筛选或泛询盘。
- `transformer_solution_reasoning`：变压器方案判断，明确显式需求和语义推断，避免关键词误判。
- `technical_parameter_audit`：报价参数体检，判断能否报价、缺什么、哪些参数不能当真。
- `missing_parameter_questioning`：缺失参数追问，生成不超过 8 条、无编号、可复制的英文追问。
- `lead_value_grading`：客户价值评级，给 A/B/C、评分、理由和跟进强度。
- `knowledge_retrieval_planning`：知识库检索计划，告诉系统和业务员应查哪些产品/认证/FAQ/历史回复资料。
- `reply_risk_control`：回复风险控制，识别价格、交期、认证、测试报告、付款条款等不可自动承诺内容。
- `sales_reply_planning`：回复策略，把分析结果转成英文草稿结构和销售下一步。

每个 Skill 输出必须包含：`priority`、`confidence`、`result`、`nextAction`、`warnings`、`evidence`。询盘详情页“AI 分析工作流”展示这些内容，用户能直接看到判断结论、下一步动作、风险提醒和依据。

DeepSeek 复测结果：明确 RFQ 仍为 A 级，产品/参数保持 `buyer_text / explicit`；室内商场场景仍为 B 级，产品方向保持 `semantic_inference / inferred / needsConfirmation=true`。两条测试均生成 8 个 Skill 结果、8 条 RAG 参考和英文回复草稿。

## 2026-09-03：首页改为客户实际使用工作台

用户要求不要再做“运营大屏”式展示，也不要展示“缺失参数排行”“上线前还缺什么”“一期能力”“资料完善”等内部交付视角内容。首页必须是客户每天打开系统就能用的工作台。

已调整为：

- `/` 首页纳入左侧管理后台布局，左侧菜单第一项为“首页”，第二项为“询盘工作台”，第三项为“AI 助手”。
- 首页风格改为浅色 CRM/SaaS 工作台，参考用户给的 Ovation 类仪表盘：白色圆角面板、柔和阴影、蓝/紫/粉业务图表、左侧菜单和顶部搜索。
- 首页只展示业务员关心的真实业务数据：最新待处理消息、本月询盘、最近已处理、待审核草稿、询盘趋势、客户质量、今日待办、产品机会、询盘来源、重点市场、最近消息、客户沉淀。
- 首页所有指标来自后端真实接口 `GET /api/inquiries`、`GET /api/customers`、`GET /api/ai-assistant/snapshot`，不写死演示数字。
- 删除旧的 `ops.css` 和旧大屏样式，避免后续误用。
- 顶栏搜索改为客户/询盘/型号搜索入口，提交后进入询盘工作台并带入搜索关键词。

后续界面原则：给客户实际使用的页面，只放日常业务动作和业务结果；内部实施清单、交付阶段、上线风险和开发说明只能放交接文档或项目记忆，不出现在客户使用界面。

## 2026-09-03：首页同步检查与偏移修正

用户说明自己又改了一些代码，要求同步当前状态并检查是否偏离需求。检查发现：

- 当前目录不是 Git 仓库，无法用 `git diff` 精确区分用户改动和系统改动，只能以当前落盘代码为准同步。
- 首页被改成 `WorldMarketMap` 地图式页面，但里面存在模拟动态：无数据时自动生成国家标记、定时随机增加询盘/成交数字，并出现“新成交 +1”“暂无新成交”“全球市场态势”等文案。由于系统当前没有订单、成交金额、成交记录表，这与“首页数据必须真实”冲突。
- 左侧菜单把首页写成“仪表盘”，并重新把“AI 模型配置”暴露在客户日常菜单里，和前面确认的客户可见口径不一致。
- `PRODUCT.md` 和 `DESIGN.md` 仍保留旧的“深色运营大屏/一期”设计记忆，会误导后续 UI 开发。

已修正：

- 修复 `demo/src/components/UserAvatar.tsx` 的错误导入路径，前端重新可构建。
- 清理 `demo/src/pages/OpsDashboardPage.tsx` 里上一版首页遗留的未使用组件，首页只保留当前地图工作台入口。
- `WorldMarketMap` 删除无数据假标记、随机增长和“成交”相关文案；最新消息改为读取真实最近询盘，点击可进入询盘详情。
- 默认主题改为浅色，保留用户手动切换深色后的本地持久化。
- 左侧菜单第一项改回“首页”，隐藏客户日常菜单里的“AI 模型配置”入口，但保留 `/ai-config` 路由以免影响已有配置能力。
- 更新 `PRODUCT.md` 和 `DESIGN.md`：后续 UI 默认按浅色 CRM/SaaS 客户工作台，不按旧的深色大屏或内部交付页面。

当前核验：

- `npm run build` 通过；Vite 仅提示 MapLibre 地图包导致 chunk 较大，后续可做路由懒加载优化。
- `npx tsc --noEmit` 通过。
- `npm run lint` 通过，仅剩 `icons.tsx` 的 Fast Refresh 规范提醒，不影响运行。
- `http://127.0.0.1:5173/` 返回 200。

## 2026-09-04：AI 助手上下文记忆与会话右键菜单

用户反馈 AI 助手没有上下文记忆，并要求会话列表支持右键删除或重命名，页面上的“删除/重命名”按钮去掉。

已调整：

- 后端 AI 助手模型调用不再只把历史对话放进一个 JSON 字段，而是把最近 12 条同一会话历史作为真实 `user/assistant` chat messages 传给 DeepSeek/OpenAI/Qwen/custom 等 Chat Completions 兼容模型。
- Claude 分支也会把同一会话历史合并进当前请求上下文。
- 系统提示明确要求理解“刚才、上一条、继续说”等连续追问，不得脱离当前会话上下文回答。
- 历史消息会截断到合理长度，避免长会话把请求撑爆。
- 前端 AI 助手左侧会话列表删除可见“重命名/删除”按钮，改为右键会话弹出菜单。
- 右键菜单支持“重命名”和“删除”；重命名仍在原位置输入，删除保留确认弹窗。

自测：

- 创建临时 AI 助手会话，第一轮输入“请记住：我今天要优先跟进 UAE 的 A 级客户。”
- 第二轮追问“我刚才说今天优先跟进谁？”
- AI 正确回答“UAE 的 A 级客户”，并结合系统真实数据补充 UAE 当前询盘情况。
- 测试会话已删除，避免污染客户数据。
