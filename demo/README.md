# Transformer Export Agent Demo

一期核心流程预览：询盘粘贴 → 参数抽取 → RAG 引用来源 → 英文回复草稿 → 人工审核。

当前为前端 Mock，不调用真实大模型。

## 启动

```bash
cd demo
npm install
npm run dev
```

浏览器打开终端提示的本地地址（通常是 `http://localhost:5173`）。
